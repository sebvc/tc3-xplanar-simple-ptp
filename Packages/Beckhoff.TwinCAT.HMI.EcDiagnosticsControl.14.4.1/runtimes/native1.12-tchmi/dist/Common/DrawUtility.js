export function createCanvasLayers(parentEl,numberOfLayers){const layers=[];for(let layerIndex=0;layerIndex<numberOfLayers;layerIndex++){const canvas=document.createElement("canvas");canvas.setAttribute("width","800px"),canvas.setAttribute("height","1000px"),canvas.classList.add("layer");const ctx=canvas.getContext("2d");null!==ctx?(layers.push({canvas,ctx}),parentEl.appendChild(canvas)):TcHmi.Log.error("canvas.getContext('2d') returned null")}return layers}export function drawGrid(ctx,numberOfRows,numberOfCols,gridSpacing){ctx.beginPath(),ctx.strokeStyle="#ddd";for(let rowIndex=0;rowIndex<numberOfRows+1;rowIndex++)ctx.moveTo(rowIndex*gridSpacing,0),ctx.lineTo(rowIndex*gridSpacing,numberOfCols*gridSpacing);for(let colIndex=0;colIndex<numberOfCols+1;colIndex++)ctx.moveTo(0,colIndex*gridSpacing),ctx.lineTo(numberOfRows*gridSpacing,colIndex*gridSpacing);ctx.stroke()}export function drawComponent(ctx,colIndex,rowIndex,width,height,size){ctx.beginPath(),ctx.fillStyle="#fff",ctx.strokeStyle="#ccc",ctx.lineWidth=.5,ctx.fillRect(colIndex*size,rowIndex*size,width*size,height*size),ctx.strokeRect(colIndex*size,rowIndex*size,width*size,height*size)}